%h1 Magazine#index
%p
  %a{ href: new_magazine_path } 📥
%p
  = button_to '↪️', magazines_import_index_path
%div
  - @magazines.each do |magazine|
    %div
      %h1
        %a{ href: magazine_path(magazine) }= magazine.title
      - if magazine.cover.present?
        %img{ src: page_path(magazine.cover) }
